var box = document.getElementsByClassName("box")[0];
box.addEventListener('onclick', function(event) {
    box.classList.add("animation-pop");
})


function pop(box) {
    box.ClassList.add("animation-pop");
    console.log("fuck")
}